{
  "triggers": [
    {
      "$extends": "heating_false"
    },
    {
      "$extends": "heating_true"
    },
    {
      "$extends": "thermostat_mode_changed"
    }
  ],
  "conditions": [
    {
      "$extends": "thermostat_mode_is"
    }
  ],
  "actions": [
    {
      "$extends": "thermostat_mode_set"
    }
  ]
}
